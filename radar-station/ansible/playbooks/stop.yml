- name: "Stop service {{app_name}} on kamatera"
  vars_files:
    - vars/prod.yml
  gather_facts: no
  hosts: kamatera
  remote_user: "{{remote_admin}}"
  become: true
  become_method: sudo
  tasks:
    - name: "Stop service {{app_name}}"
      systemd:
        state: stopped
        name: "{{app_name}}"
