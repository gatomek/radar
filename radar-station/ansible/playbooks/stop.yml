- name: Stop service radar-station
  gather_facts: no
  hosts: servers
  remote_user: "{{remote_admin}}"
  become: true
  become_method: sudo
  tasks:
    - name: "Stop service {{app_name}}"
      systemd:
        state: stopped
        name: "{{app_name}}"
